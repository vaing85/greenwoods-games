{
  "name": "greenwood-games",
  "version": "1.0.0",
  "description": "Greenwood Games - A comprehensive casino game platform with web and mobile interfaces",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run web:dev\"",
    "server:dev": "Set-Location server; npm run dev",
    "web:dev": "Set-Location web; npm start",
    "mobile:dev": "Set-Location mobile; npm start",
    "install:all": "npm install; Set-Location server; npm install; Set-Location ../web; npm install; Set-Location ../mobile; npm install",
    "build": "Set-Location web; npm run build",
    "start": "Set-Location server; npm start"
  },
  "keywords": ["casino", "games", "react", "react-native", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
